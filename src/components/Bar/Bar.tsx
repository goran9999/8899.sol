import React from "react";
import "./Bar.scss";
const Bar = () => {
  return (
    <div className="bar">
      To use this app, local validator must be running on port 8899!
    </div>
  );
};

export default Bar;
