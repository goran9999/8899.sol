export enum RpcConnection {
  Localhost,
  Devnet,
  Mainnet,
}

export enum AccountType {
  PublicKey,
  Keypair,
}

export enum KeypairType {
  UserDefine,
  Grind,
}

export enum ProgramInfoType {
  Accounts,
  Instructions,
  Events,
}
